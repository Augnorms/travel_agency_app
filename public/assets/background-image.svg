<svg xmlns="http://www.w3.org/2000/svg"
     viewBox="0 0 1400 300"
     preserveAspectRatio="xMidYMid meet"
     style="width:100%; height:auto; display:block;">

  <defs>
    <linearGradient id="bgGradient" x1="0" x2="1" y1="0" y2="0">
      <stop offset="0%" stop-color="#0057B8" />
      <stop offset="100%" stop-color="#3399FF" />
    </linearGradient>

    <pattern id="circlePattern" width="40" height="40" patternUnits="userSpaceOnUse">
      <circle cx="20" cy="20" r="2" fill="white" opacity="0.1" />
    </pattern>

    <filter id="logoGlow" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur in="SourceGraphic" stdDeviation="4" result="blur"/>
      <feMerge>
        <feMergeNode in="blur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <style>
      /* Responsive sizes using clamp(min, preferred, max).
         preferred uses viewport units so it shrinks smoothly. */
      .logoText {
        font-family: Arial, sans-serif;
        font-size: clamp(36px, 4.5vw, 72px); /* min 36, scales, max 72 */
        font-weight: bold;
        fill: white;
      }
      .superscript {
        font-size: clamp(12px, 1.6vw, 28px);
        baseline-shift: super;
      }
      .commission {
        font-family: Arial, sans-serif;
        font-size: clamp(16px, 2.2vw, 36px); /* min 16, scales, max 36 */
        fill: white;
        font-weight: 600;
        text-anchor: middle;
      }

      /* Optional fallback/resizing using media queries for older browsers */
      @media (max-width: 1000px) {
        .logoText { font-size: 60px; }
        .superscript { font-size: 24px; }
        .commission { font-size: 30px; }
      }
    </style>
  </defs>

  <rect width="1400" height="300" fill="url(#bgGradient)" />
  <rect width="1400" height="300" fill="url(#circlePattern)" />
  <path d="M0,300 L0,240 C300,180 600,220 900,180 L1400,100 L1400,300 Z" fill="white" opacity="0.15" />

  <g transform="translate(700,150)" text-anchor="middle" filter="url(#logoGlow)">
    <circle cx="0" cy="-40" r="80" fill="#0057B8" stroke="white" stroke-width="3"/>
    <text x="0" y="-45" class="logoText">
      S<tspan class="superscript" dx="-8" dy="-20">2</tspan>
    </text>
    <text x="0" y="10" class="logoText">
      C<tspan class="superscript" dx="-8" dy="-20">2</tspan>
    </text>
  </g>
</svg>
